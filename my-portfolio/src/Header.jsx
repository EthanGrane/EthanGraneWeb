export default function Header()
{
    return(
        <header className="fixed w-full h-[64px] bg-black">
            <nav>
                <a href="www.google.com">Google</a>
            </nav>
        </header>
    );
}